[phases.setup]
nixPkgs = ["nodejs", "ruby_3_2", "postgresql"]

[phases.install]
cmds = ["cd backend && bundle install"]

[phases.build]
cmds = ["bundle install && bundle exec rails assets:precompile"]

[start]
cmd = "bundle exec rails server -b 0.0.0.0 -p $PORT"